/*
 * Локальная конфигурация Supabase.
 * Допустим только клиентский publishable/anon key — никогда service_role/secret.
 */
window.AIDEMA_SUPABASE_CONFIG = Object.freeze({
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
});
