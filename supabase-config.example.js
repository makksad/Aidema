/*
 * Скопируйте этот файл как supabase-config.js и заполните клиентские данные
 * из Supabase Dashboard → Project Settings / API Keys.
 *
 * Никогда не вставляйте сюда service_role или secret key.
 */
window.AIDEMA_SUPABASE_CONFIG = Object.freeze({
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
});
